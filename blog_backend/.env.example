# Database connection string (MongoDB Atlas or local)
DB_URL=mongodb+srv://user:password@cluster.mongodb.net/blog

# Secret key for JWT token signing (use a strong random string)
JWT_SECRET=your-secret-key-here

# Server port
PORT=4444
